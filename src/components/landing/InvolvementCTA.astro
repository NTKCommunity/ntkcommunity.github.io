<section class="py-16">
  <div class="max-w-7xl mx-auto px-4 md:px-10 grid grid-cols-1 md:grid-cols-2 gap-12 items-center">

    <!-- Left: Animated Image with multiple animations -->
    <div class="flex justify-center md:justify-start">
      <img src="/assets/images/ntc_website-pic01.png" alt="Bot" class="w-72 floating-robot spin-float" />
    </div>

    <!-- Right: Text Content with enhanced animations -->
    <div class="text-center md:text-left">
      <p class="text-blue-600 font-semibold text-lg mb-2 animate-text-pop">
        We're a dynamic team of
      </p>
      <h1 class="text-3xl md:text-5xl font-bold text-gray-900 leading-tight mb-4">
        <span class="animate-word-bounce">Innovative,</span> <span class="text-blue-600 animate-tech-spin">Tech</span><br />
        <span class="animate-word-wave">Enthusiastic People</span>
      </h1>
       <p class="text-gray-700 text-base md:text-lg mb-2 animate-fade-in-up delay-100">
        At Nepal Tek, we're more than just a community; we're a gathering of minds fueled by a shared passion for technology
      </p>
      <p class="text-gray-700 text-base md:text-lg mb-6 animate-fade-in-up delay-200">
       join us in shaping the future of tech in Nepal and beyond.
      </p> 
      <a href="#" class="inline-block bg-blue-600 text-white px-6 py-3 rounded-full shadow hover:bg-blue-700 transition hover:scale-105 animate-pulse-slow">
        Learn more →
      </a>
    </div>

  </div>
</section>

<style>
  /* Base fade-in animation */
  @keyframes fadeInUp {
    0% { opacity: 0; transform: translateY(20px); }
    100% { opacity: 1; transform: translateY(0); }
  }
  
  .animate-fade-in-up {
    animation: fadeInUp 0.8s ease-out forwards;
  }
  
  .delay-100 {
    animation-delay: 0.1s;
  }
  
  .delay-200 {
    animation-delay: 0.2s;
  }

  /* Robot animations */
  @keyframes float {
    0%, 100% { transform: translateY(0) rotate(-2deg); }
    50% { transform: translateY(-20px) rotate(2deg); }
  }
  
  @keyframes spin {
    0% { transform: rotate(0deg); }
    100% { transform: rotate(360deg); }
  }
  
  .floating-robot {
    animation: float 4s ease-in-out infinite;
  }
  
  .spin-float:hover {
    animation: float 2s ease-in-out infinite, spin 8s linear infinite;
  }

  /* Text animations */
  @keyframes textPop {
    0% { transform: scale(0.9); opacity: 0; }
    80% { transform: scale(1.05); }
    100% { transform: scale(1); opacity: 1; }
  }
  
  .animate-text-pop {
    animation: textPop 0.6s ease-out forwards;
  }
  
  @keyframes bounce {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-10px); }
  }
  
  .animate-word-bounce {
    display: inline-block;
    animation: bounce 2s ease-in-out infinite;
  }
  
  @keyframes wave {
    0%, 100% { transform: translateY(0) rotate(0deg); }
    25% { transform: translateY(-5px) rotate(2deg); }
    75% { transform: translateY(5px) rotate(-2deg); }
  }
  
  .animate-word-wave {
    display: inline-block;
    animation: wave 3s ease-in-out infinite;
  }
  
  @keyframes techSpin {
    0% { transform: rotate(0deg) scale(1); }
    50% { transform: rotate(180deg) scale(1.1); }
    100% { transform: rotate(360deg) scale(1); }
  }
  
  .animate-tech-spin {
    display: inline-block;
    animation: techSpin 4s linear infinite;
  }
  
  @keyframes pulseSlow {
    0%, 100% { transform: scale(1); }
    50% { transform: scale(1.05); }
  }
  
  .animate-pulse-slow {
    animation: pulseSlow 3s ease-in-out infinite;
  }
</style>


